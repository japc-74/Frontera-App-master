/*
import { CUSTOM_ELEMENTS_SCHEMA } from '@angular/core';
import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { MovementDocumentationPage } from './movement-documentation.page';

describe('MovementDocumentationPage', () => {
  let component: MovementDocumentationPage;
  let fixture: ComponentFixture<MovementDocumentationPage>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ MovementDocumentationPage ],
      schemas: [CUSTOM_ELEMENTS_SCHEMA],
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(MovementDocumentationPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
*/